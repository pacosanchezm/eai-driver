import React from "react";
import ReactDOM from "react-dom";

import App from "../eai-driver/appi";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <App 
     // id={3654898}
      id={2437933}

    />
  </React.StrictMode>,
  rootElement
);
